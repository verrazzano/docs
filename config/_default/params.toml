# Verrazzano _default site params

# The version number for the version of the product referenced by this doc set.
# Used by short codes release_asset_url and release_source_url which are defined files in layouts/shortcodes
release_version = "v1.6.11"

# Version number used in Verrazzano release assets. For example, 1.5.0 in verrazzano-1.5.0-linux-amd64.tar.gz
download_package_version = "1.6.11"

# Full version number used as part of downloaded artifact when downloading from Oracle Software Delivery Cloud
# Do not change this value for a patch release
download_package_full_version = "1.6.0.0.0"

opensearch_docs_url = "https://opensearch.org/docs/2.3"

copyright = "Oracle and/or its affiliates"
#privacy_policy = "https://policies.google.com/privacy"

# Menu title if your navbar has a versions selector to access old versions of your site.
# This menu appears only if you have at least one [params.versions] set.
version_menu = "v1.6"

# Flag used in the "version-banner" partial to decide whether to display a 
# banner on every page indicating that this is an archived version of the docs.
# Set this flag to "true" if you want to display the banner.
archived_version = false

# The version number for the version of the docs represented in this doc set.
# Used in the "version-banner" partial to display a version number for the 
# current doc set.
version = "v1.6.11"

# A link to latest version of the docs. Used in the "version-banner" partial to
# point people to the main doc site.
url_latest_version = "https://verrazzano.github.io/docs/docs"

# An optional link to a related project repo. For example, the sibling repository where your product code lives.
github_project_repo = "https://github.com/verrazzano/verrazzano"

# The release branch, used by ghlink.html and release_branch.html which are defined files in layouts/shortcodes
github_branch = "release-1.6"

# Specify a value here if your content directory is not in your repo's root directory
# github_subdir = ""

# The version of the Kubernetes API that is used to link to the existing API documentation
# for Kubernetes resources
kubernetes_api_version = "v1.24"

# Enable Algolia DocSearch
algolia_docsearch = false

# Enable Lunr.js offline search
offlineSearch = true
offlineSearchSummaryLength = 200

# Jaeger version number to be used for Jaeger documentation links
jaeger_doc_version = "1.42"

# Rancher version to be used for Rancher documentation links
rancher_doc_version = "v2.7"

# Verrazzano release version
verrazzano_development_version = "1.6.11"

# CAPOCI release version
capoci_version = "0.9.0"

# CAPOCNE release version
capocne_version = "1.6.1"

# disable the default click to copy feature
disable_click2copy_chroma = true

# User interface configuration
[ui]
# Enable to show the side bar menu in its compact state.
sidebar_menu_compact = true
ul_show = 1
sidebar_menu_foldable = true
sidebar_cache_limit = 1000

#  Set to true to disable breadcrumb navigation.
breadcrumb_disable = false
#  Set to true to hide the sidebar search box (the top nav search box will still be displayed if search is enabled)
sidebar_search_disable = false
#  Set to false if you don't want to display a logo (/assets/icons/logo.svg) in the top nav bar
navbar_logo = true
# Set to true to disable the About link in the site footer
footer_about_disable = false
# Enable/disable translucent navbar
navbar_translucent_over_cover_disable = true

# Adds a H2 section titled "Feedback" to the bottom of each doc. The responses are sent to Google Analytics as events.
# This feature depends on [services.googleAnalytics] and will be disabled if "services.googleAnalytics.id" is not set.
# If you want this feature, but occasionally need to remove the "Feedback" section from a single page,
# add "hide_feedback: true" to the page's front matter.
[ui.feedback]
enable = true
# The responses that the user sees after clicking "yes" (the page was helpful) or "no" (the page was not helpful).
yes = 'Glad to hear it! Please <a href="https://github.com/verrazzano/docs/issues/new">tell us how we can improve</a>.'
no = 'Sorry to hear that. Please <a href="https://github.com/verrazzano/docs/issues/new">tell us how we can improve</a>.'

# Adds a reading time to the top of each doc.
# If you want this feature, but occasionally need to remove the Reading time from a single page, 
# add "hide_readingtime: true" to the page's front matter
[ui.readingtime]
enable = false


[katex]
enable = true

[links]
# End user relevant links. These will show up on left side of footer and in the community page if you have one.
[[links.user]]
	name ="Twitter"
	url = "https://twitter.com/verrazzano_io"
	icon = "fab fa-twitter"
        desc = "Follow us on Twitter to get the latest news!"
[[links.user]]
	name = "Stack Overflow"
	url = "https://stackoverflow.com/tags/verrazzano"
	icon = "fab fa-stack-overflow"
        desc = "Practical questions and curated answers"
# Developer relevant links. These will show up on right side of footer and in the community page if you have one.
[[links.developer]]
	name = "GitHub"
	url = "https://github.com/verrazzano/verrazzano"
	icon = "fab fa-github"
        desc = "Development takes place here!"
[[links.developer]]
	name = "Slack"
	url = "https://bit.ly/3gOeRJn"
	icon = "fab fa-slack"
        desc = "Chat with other project developers"

